# Migration `watch-20191121161527`

This migration has been generated by alechp at 11/21/2019, 4:15:27 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "csat"."TwitterUserMeta" (
  "id" text NOT NULL  ,
  "twitterId" text NOT NULL DEFAULT '' ,
  "twitterUsername" text NOT NULL DEFAULT '' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "csat"."User" (
  "createdAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "email" text   ,
  "id" text NOT NULL  ,
  "name" text   ,
  "updatedAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "csat"."Prediction" (
  "attributed" boolean NOT NULL DEFAULT false ,
  "content" text NOT NULL DEFAULT '' ,
  "createdAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "finish" timestamp(3)   ,
  "id" text NOT NULL  ,
  "start" timestamp(3)   ,
  "status" text NOT NULL DEFAULT 'Active' ,
  "updatedAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "csat"."Asset" (
  "createdAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "id" text NOT NULL  ,
  "name" text NOT NULL DEFAULT '' ,
  "symbol" text NOT NULL DEFAULT '' ,
  "updatedAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "csat"."AssetPair" (
  "createdAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "id" text NOT NULL  ,
  "ratio" Decimal(65,30) NOT NULL DEFAULT 0 ,
  "updatedAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "csat"."Response" (
  "createdAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "id" text NOT NULL  ,
  "sentAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "status" text NOT NULL DEFAULT 'UNPARSED' ,
  "type" text NOT NULL DEFAULT 'ACKNOWLEDGMENT' ,
  "updatedAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY ("id")
);

ALTER TABLE "csat"."User" ADD COLUMN "twitter" text   REFERENCES "csat"."TwitterUserMeta"("id") ON DELETE SET NULL;

ALTER TABLE "csat"."Prediction" ADD COLUMN "assetPair" text   REFERENCES "csat"."AssetPair"("id") ON DELETE SET NULL,
ADD COLUMN "author" text   REFERENCES "csat"."User"("id") ON DELETE SET NULL;

ALTER TABLE "csat"."Asset" ADD COLUMN "assetNumerator" text   REFERENCES "csat"."AssetPair"("id") ON DELETE SET NULL,
ADD COLUMN "currencyDenominator" text   REFERENCES "csat"."AssetPair"("id") ON DELETE SET NULL;

ALTER TABLE "csat"."Response" ADD COLUMN "prediction" text   REFERENCES "csat"."Prediction"("id") ON DELETE SET NULL,
ADD COLUMN "recipient" text   REFERENCES "csat"."User"("id") ON DELETE SET NULL;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration watch-20191121161331..watch-20191121161527
--- datamodel.dml
+++ datamodel.dml
@@ -5,9 +5,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = "postgresql://alechp@localhost:5432/csat?schema=csat"
 }
 // --------------------------
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191121161527)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191121161527'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
